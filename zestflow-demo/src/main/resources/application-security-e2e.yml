# 与 Admin application-security-e2e.yml 成对；须同时重启 Admin + Executor
zestflow:
  executor:
    access-token: e2e-security-executor-token
    registry-token: e2e-security-registry-token
  collector:
    access-token: e2e-security-collector-token
