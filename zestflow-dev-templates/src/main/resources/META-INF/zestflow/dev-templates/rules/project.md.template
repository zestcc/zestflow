# {{APP_CODE}} 项目级 Dev Copilot 规则（L2）

> L1 平台规范已内置在 `zestflow-mcp.jar`；本文件仅描述**本项目差异**。

## 包与目录

- 业务元件放在 `{{BASE_PACKAGE}}.component`
- 新元件类名以 `Handler` 或 `Component` 结尾
- 包路径: {{BASE_PACKAGE}}.component

## 命名

- `componentId` 使用 camelCase
- 默认 appCode: **{{APP_CODE}}**

## Dev Copilot（MCP）

- 平台 JAR 一次安装：`~/.zestflow/tools/zestflow-mcp.jar`（见 ZestFlow 文档 MCP_SETUP）
- 本项目 MCP 配置：`.cursor/mcp.json`（Cursor）或 `.zestflow/mcp/` 下其他 IDE 示例
- 重新生成：`java -jar ~/.zestflow/tools/zestflow-mcp.jar --init-dev --project . --force`

## Chain-first 学习（L2 项目）

- 学习事件：`.zestflow/learning/events.jsonl`（原始信号，建议 gitignore）
- 蒸馏 Pattern：`.zestflow/patterns/`（建议提交 Git 团队继承）

## 禁止

- 不要自动提交 Git 或调用 Admin 发布 API
- 源码落盘使用 IDE Apply，不用 MCP 写盘
