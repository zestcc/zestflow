server:
  port: 8081

spring:
  application:
    name: demo-app
  config:
    import: optional:classpath:application-local.yml
  # 禁用 Flyway（所有 DDL 在 init.sql 中管理）
  flyway:
    enabled: false

zestflow:
  executor:
    port: 20550
  playground:
    # 当前服务的基础 URL（用于 Admin 试验场拼接完整请求路径），按实际部署地址修改
    url: http://localhost:8081
    scan-packages:
      - com.zestflow.test.controller
  collector:
    registry:
      port: 20650
