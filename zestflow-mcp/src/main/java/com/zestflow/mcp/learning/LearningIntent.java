package com.zestflow.mcp.learning;

/**
 * Chain-first 工作流意图（关键字映射见 {@link LearningWorkflow}）。
 */
public enum LearningIntent {
    PLAN_CHAIN,
    SCAFFOLD_COMPONENT,
    COMPOSE_CHAIN,
    BIND_HTTP,
    GEN_PLAYGROUND_SCENE,
    VERIFY,
    RECORD_FEEDBACK,
    DISTILL_PATTERNS,
    SEARCH_PATTERNS,
    SHARE_PATTERN
}
